<%= turbo_frame_tag "cart" do %>
  <h4 class="pt-4">Shopping Cart</h4>
  <% if @cart.orderables.count > 0 %>
    <%= render "partials/orderables" %>
    <hr>
    <p>Total: <%= @cart.total%></p>
  <%else %>
    <p>Your cart is empty</p>
  <%end%>
<%end%>